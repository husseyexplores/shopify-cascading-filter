{
  "name": "x-ymm-filter",
  "description": "Year, Model, Make filtering for Shopify (native)",
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "rm -rf ./dist/**",
    "build": "pnpm run prebuild && node ./build.mjs",
    "minify-build": "node ./esbuild.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.17.5",
    "eslint": "^8.33.0",
    "eslint-config-prettier": "^8.6.0",
    "eslint-plugin-import": "^2.27.5",
    "prettier": "^2.8.3",
    "vite": "^4.0.4"
  },
  "dependencies": {
    "@maverick-js/signals": "^5.9.1",
    "stable-hash": "^0.0.3"
  }
}
